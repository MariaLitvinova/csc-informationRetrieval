Индексатор
На вход индексатору подаётся два параметра: путь до папки с документами, которые требуется проиндексировать, и путь до MyStem.exe. Индексатор делает следующее:

- обрабатывает коллекцию документов
- создаёт папку index рядом с исполняемым файлом
- кладёт в эту папку файл с индексом